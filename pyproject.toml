[project]
name = "wibt-tool"
dynamic = ["version"]
requires-python = ">= 3.14"
dependencies = [
  "dotenv",
  "requests",
  "openai",
  "marimo",
  "altair",
  "pandas",
  "pyarrow",
  "pydantic",
  "matplotlib",
  "plotly",
  "docling",
  "pydantic-ai>=1.105.0",
]

[tool.marimo.runtime]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
